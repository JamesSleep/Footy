header.header
    .header__wrapper
        .header__title
            a(href=routes.home)
                span MAESTRO
        .header__column
            form(action=routes.search method="get")
                input(type="text", placeholder="팀명을 검색해보세요", name="term" )
        .header__column
            ul
                if !user.isAuthenticated
                    li
                        a(href=routes.join) 회원가입
                    li
                        a(href=routes.login) 로그인
                else
                    li
                        a(href=`/matches${routes.addMatch}`) 경기추가
                    li
                        a(href=routes.userDetail(user.id)) 프로필
                    li
                        a(href=routes.logout) 로그아웃
